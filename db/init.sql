CREATE TABLE shelf_a (
    id SERIAL PRIMARY KEY,
    bin_num TEXT,
    name TEXT,
    price NUMERIC (10, 2),
    image TEXT
);

CREATE TABLE shelf_b (
    id SERIAL PRIMARY KEY,
    bin_num TEXT,
    name TEXT,
    price NUMERIC (10, 2),
    image TEXT
);

CREATE TABLE shelf_c (
    id SERIAL PRIMARY KEY,
    bin_num TEXT,
    name TEXT,
    price NUMERIC (10, 2),
    image TEXT
);

CREATE TABLE shelf_d (
    id SERIAL PRIMARY KEY,
    bin_num TEXT,
    name TEXT,
    price NUMERIC (10, 2),
    image TEXT
);